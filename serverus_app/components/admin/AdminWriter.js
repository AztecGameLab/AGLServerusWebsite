import React from 'react';

export default class AdminWriter extends React.Component{
    render(){
        return(
            <div>
                This is the writer page.
            </div>
        );
    }
}