import React from 'react';
import { Grid, Image } from 'semantic-ui-react'

const Footer = (props) => {
    return (
        <div>
            test footer {props.name}
        </div>
    );
};

export default Footer;