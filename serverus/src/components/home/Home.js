import React, { Component } from "react";

//Components
import ReactPlayer from "react-player";

//Stylings
import "./Home.css";

class Home extends Component {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

// Inline Styles
const headerText = {
  fontSize: "4em",
  fontWeight: "normal"
};

const firstPart = {
  height: "700px",
  textAlign: "center"
};

const secondPart = {
  background: "#FFFFFF",
  padding: "8em"
};

const thirdPart = {
  background: "#FFFFFF",
  padding: "6em"
};

export default Home;
